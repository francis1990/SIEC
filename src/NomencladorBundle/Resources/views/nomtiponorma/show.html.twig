{% extends 'base.html.twig' %}

{% block body %}
    <h1>NomTipoNorma</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nomTipoNorma.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ nomTipoNorma.codigo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ nomTipoNorma.nombre }}</td>
            </tr>
            <tr>
                <th>Alias</th>
                <td>{{ nomTipoNorma.alias }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('nomtiponorma_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('nomtiponorma_edit', { 'id': nomTipoNorma.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
