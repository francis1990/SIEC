{% extends 'base.html.twig' %}

{% block body %}
    <h1>NomPrecio</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nomPrecio.id }}</td>
            </tr>
            <tr>
                <th>Código</th>
                <td>{{ nomPrecio.codigo }}</td>
            </tr>
            <tr>
                <th>Preciomn</th>
                <td>{{ nomPrecio.preciomn }}</td>
            </tr>
            <tr>
                <th>Preciocuc</th>
                <td>{{ nomPrecio.preciocuc }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{% if nomPrecio.activo %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('precio_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('precio_edit', { 'id': nomPrecio.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
