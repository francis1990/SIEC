{% extends 'base.html.twig' %}

{% block body %}
    <h1>NomTipotransporte</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nomTipotransporte.id }}</td>
            </tr>
            <tr>
                <th>Código</th>
                <td>{{ nomTipotransporte.codigo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ nomTipotransporte.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ nomTipotransporte.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipotransporte_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tipotransporte_edit', { 'id': nomTipotransporte.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
