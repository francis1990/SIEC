{% extends 'base.html.twig' %}

{% block body %}
    <h1>NomMonedadestino</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ nomMonedadestino.id }}</td>
        </tr>
        <tr>
            <th>Código</th>
            <td>{{ nomMonedadestino.codigo }}</td>
        </tr>
        <tr>
            <th>Nombre</th>
            <td>{{ nomMonedadestino.nombre }}</td>
        </tr>

        </tbody>
    </table>

    <ul>
        <li>
            <a title="Back to the list" href="{{ path('monedadestino_index') }}">Back to the list</a>
        </li>
        <li>
            <a title="Edit" href="{{ path('monedadestino_edit', { 'id': nomMonedadestino.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
