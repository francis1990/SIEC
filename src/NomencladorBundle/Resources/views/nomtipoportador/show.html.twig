{% extends 'base.html.twig' %}

{% block body %}
    <h1>NomTipoPortador</h1>

    <table>
        <tbody>
            <tr>
                <th>Idtipoportador</th>
                <td>{{ nomTipoPortador.idtipoportador }}</td>
            </tr>
            <tr>
                <th>Código</th>
                <td>{{ nomTipoPortador.codigo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ nomTipoPortador.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipoportador_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tipoportador_edit', { 'id': nomTipoPortador.idtipoportador }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
