{% extends 'base.html.twig' %}

{% block body %}
    <h1>DatIncidencia</h1>

    <table>
        <tbody>
            <tr>
                <th>Idincidencia</th>
                <td>{{ datIncidencium.idincidencia }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ datIncidencium.numero }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ datIncidencium.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a title="Back to the list" href="{{ path('incidencia_index') }}">Back to the list</a>
        </li>
        <li>
            <a title="Edit" href="{{ path('incidencia_edit', { 'id': datIncidencium.idincidencia }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
