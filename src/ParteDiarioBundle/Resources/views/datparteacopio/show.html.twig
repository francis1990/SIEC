{% extends 'base.html.twig' %}

{% block body %}
    <h1>DatParteAcopio</h1>

    <table>
        <tbody>
            <tr>
                <th>Idparte</th>
                <td>{{ datParteAcopio.idparte }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{% if datParteAcopio.fecha %}{{ datParteAcopio.fecha|date('Y-m-d') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('parteacopio_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('parteacopio_edit', { 'id': datParteAcopio.idparte }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
